# 0.3.0 packaging — main-agent review

Base: 05c7b6b9ab941cd1de2c930d5854b9946531e213.
Scope: existing release consumers, immutable stable inputs, version metadata,
and macOS process-test fixture. No tunnel behavior or runtime architecture rewrite.

## Findings addressed

- CI runner-death test failed waiting for the Python fixture, before the actual
  guardian assertion. Replaced Python with a child of the Rust test executable;
  retained all deadlines, lease assertions, and bounded orphan cleanup.
- Android common host's updater used its own crate version. Added that crate to
  version updates, bumped it to 0.3.0, and added a failing-then-passing regression.
- Shipping staging/signing now distinguish current 0.3.0 source/root from the
  pinned published 0.2.20 source/root. Missing, substituted or wrong-source stable
  inputs fail before exposing output. Native staging compares extracted bytes.
- Android links the archived AAR for real two-slot shipping; final package checks
  retain one common VPN service and verify both compiled namespaces and ELF bytes.

## Verification

- Rust workspace: 1,025 passed, 1 ignored; Clippy with warnings denied passed.
- Python discovery: 140 tests, OK, 5 platform/opt-in skips.
- UI: 121 passed; Svelte checks/build passed.
- Android app + tunnel JVM tests: 769 passed, no failures/errors/skips.
- macOS runner-death fixture: 30 consecutive repetitions passed.
- Release workflow checker passed; published stable root/signatures and all four
  archive hashes authenticated against the configured release public pin.
- Panel updater job 3619 completed at 2026-09-19 20:49:11 UTC, target ec6ce12;
  schema unchanged, service active, no subsequent error-priority journal entries.

## Verdict and remaining checks

Self-review by the main agent, as requested; no reviewer subagent. No remaining
blocking findings in this change. Ready for **candidate build**, not a claim of
release or hardware acceptance. Native CI builds, exact signed APK verification,
upgrade installation and Android tunnel tests remain pending.

Before installation/first Stray start, capture live logs on the USB-connected
S23 Ultra and the involved Stray servers with UTC markers. Do not clear logs,
uninstall the app, modify server configuration or retry a failed handshake
before preserving its evidence. Stable 0.2.20 remains published and unchanged.

## Follow-up: standalone Linux diagnostic

The manually dispatched `checks.yml` job is a Linux-only test AppImage build,
not the ordinary macOS/checks matrix. Its old 0.2.20 source gate and draft paths
were incompatible with current main. Two regression tests reproduced both
failures, then passed after version propagation. Full Python discovery now:
142 tests, OK, 5 skips. This follow-up changes only the diagnostic workflow,
its packaging helper, tests and this report — no application/runtime sources
or release-candidate consumers. The already running signed candidate remains
pinned to d8a7300d5b7579d1a94bdc356487c7aa25454024.

## Follow-up: native Android candidate gate

Candidate 35469287630 failed before signing: the new 12 C JNI exports for
`JniRedundantNativeApi` were not namespaced in the generated isolated runtime.
The Kotlin namespace was already isolated. Extended the existing C name rewrite
to cover `Java_ru_nelomai_tunnel_`; no collision-check bypass or runtime behavior
change. Published 0.2.20 inputs remain byte-identical and are not rebuilt.

Regression: extract all 12 actual names from the parent patch, compile Android
ELF and inspect exported symbols; RED before the helper, GREEN after. Full Python
discovery: 143 tests OK, 5 skips. Additionally built both real ARM64 Go/JNI
libraries with verified Go 1.25.0 and NDK 28.2: 25 JNI exports each, 12 redundant
exports each, zero intersections; every isolated export has the stable prefix.
Main-agent review found no remaining blocker in this fix. A fresh source-pinned
candidate run is required; the failed run produced no signed APK. Standalone
Linux diagnostic 35469707547 completed successfully.
