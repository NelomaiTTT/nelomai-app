# Nelomai 0.2.20 — план исправлений, проверки и выпуска

> **For agentic workers:** Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Выполнять последовательно в текущей задаче; делегирование отдельно не согласовано.

**Goal:** Исправить NLM-018, NLM-020, NLM-021, NLM-023 и NLM-024; проверить обновление с подключённым ADB и выпустить проверенный пакет 0.2.20.

**Architecture:** Узкие изменения существующих frontend, core stop, Android dispatcher/tile и desktop IPC authorization. Для NLM-019 — сбор свидетельств при штатном обновлении; найденный дефект обработки отмены рассматривается отдельно от уже доказанного NLM-020. Существующая схема sign_candidate → publish_approved_candidate сохраняется.

**Tech Stack:** Svelte/TypeScript/Vitest, Rust/Tokio, Kotlin/Android/Robolectric, Unix sockets, Windows named pipes, GitHub Actions, ADB.

**Spec:** Согласованный в разговоре состав 0.2.20 и реестр `/Users/altzxd/Documents/Panel/KNOWN-ISSUES.md`, разделы NLM-018…021, NLM-023, NLM-024. Это план точечных исправлений существующих механизмов, без новой архитектуры.

## Ограничения и исходное состояние

- Рабочий checkout: `/Users/altzxd/Documents/GitHub/nelomai-app-0.2.16-maintenance`, ветка `codex/confirmed-stable-0.2.16`; исходная версия 0.2.19. Перед началом зафиксировать фактический HEAD.
- При составлении плана имеются чужие изменения внутри `vendor/amneziawg-android`, `vendor/amneziawg-go` и untracked `scripts/android/check-startup-smoke.py`. Не включать их автоматически; сборку выполнять из проверенного снимка коммита с закреплёнными submodules.
- Поддерживать WG и AWG; сохранять аккаунт, правила split tunnel, исключение локальной сети, реальный logout, отзыв сессии и ограничения перехода latest/stable.
- Не добавлять переключение stable пользователю 0.2.20: это отдельная работа для 0.3.0.
- Проверять каждое исправление отдельно и затем их взаимодействие. Успешные проверки повторять после новых изменений, затрагивающих проверенную область.
- Никаких токенов, ключей и паролей в новой диагностике. Фиксировать коды, фазы, причины и идентификаторы операций.
- Панель, агенты и kernel-пилот не входят в продуктовые изменения этого выпуска. Панель используется для штатного предложения обновления и сверки переходов/журналов.
- Планирование не означает выполненный push, запуск сборки или публикацию. При исполнении учитывать уже выданные пользователем команды; не запрашивать их повторно.
- Публиковать тот же подписанный кандидат, который прошёл аппаратную проверку. Изменение байтов после проверки требует нового кандидата и проверки затронутых сценариев.

## Порядок и контрольные точки

1. NLM-018 — актуальность статуса runtime.
2. NLM-020 — остановка для обновления и состояние авторизации.
3. NLM-021 — принятый запрос плитки и отложенная очистка процесса.
4. NLM-023 — недоступность владельца runtime и очередь чтения.
5. NLM-024 — стоимость проверки executable в desktop dispatcher.
6. Диагностика NLM-019 и совместное ревью.
7. Версия, changelog, проверки и подписанный кандидат.
8. Аппаратная проверка Android и desktop.
9. Публикация и проверка распространения.

Каждый блок заканчивается записанным результатом: коммит/изменённые файлы, выполненная проверка, её результат, оставшееся ограничение. Продуктовые исправления вести отдельными коммитами; коммиты не равны push.

### Задача 1. NLM-018: обновлять статус после подготовки runtime

**Файлы:** `src/routes/+page.svelte`, `src/lib/app-model.ts`, `src/lib/app-model.test.ts`; при необходимости создать `src/lib/runtime-status-refresh.ts` и одноимённый `.test.ts` для реально используемой логики согласования запросов.

**Вход:** завершение bootstrap, ответы runtime status, события видимости. **Выход:** актуальная блокировка кнопки старта, учитывающая реальный переход.

- [ ] Воспроизвести регрессию: ранний `auth_resuming` → успешный bootstrap → серверный `complete`, окно остаётся открытым. До исправления старт остаётся заблокированным.
- [ ] Перечитывать runtime status после успешной подготовки; при незавершённой фазе обеспечивать ограниченный по частоте повтор. Не создавать второй конкурирующий постоянный опрос.
- [ ] Защитить состояние от запоздавшего старого ответа: `complete` не должен заменяться ранее запрошенным `auth_resuming`.
- [ ] Проверить ошибку запроса/повтор, закрытие компонента, реальный switch, выбранную другую версию и требование перезапуска. Не разрешать старт только по успеху bootstrap без актуального допуска.
- [ ] Выполнить `npm run test:unit` и `npm run check`; записать результат и отревьюить diff.

**Приёмка:** после завершения перехода кнопка становится доступной без перезапуска; реальная блокировка перехода продолжает действовать.

### Задача 2. NLM-020: остановка для обновления сохраняет смысл авторизации

**Файлы:** `crates/client-core/src/lib.rs`, `crates/client-container/src/runtime_auth.rs`, `crates/client-container/src/ipc/child_admission.rs`, `src-tauri/src/updates.rs`, `src/routes/+page.svelte`, `src/lib/app-model.ts`.

**Тесты:** `crates/client-container/tests/update.rs`, `crates/client-container/tests/core_auth.rs`, `crates/client-core/tests/auth_boundary.rs`, `src/lib/app-model.test.ts`.

**Вход:** штатный `UpdateBarrier` и подтверждённая локальная остановка. **Выход:** остановленный VPN и корректный экран без ложного logout.

- [ ] Воспроизвести на общем состоянии core путь `stop_local` → `CoreState::default()` → SignedOut при сохранённых credentials.
- [ ] Закрепить оба UI-случая: SignedOut/None и SignedOut/Recovering, включая поздний ответ старого опроса.
- [ ] Развести смысл служебной остановки и настоящей потери авторизации в существующем пути вызова. До выбора точной правки сверить всех потребителей `stop_local`, особенно revoke/logout/runtime switch. Не заменять универсальный default на Ready.
- [ ] После подтверждённой остановки согласовать отображение соединения; не показывать активный туннель на основании старого снимка. При неудачной остановке сохранить ошибку и запрет установки без подтверждения.
- [ ] Проверить logout и отзыв токена во время остановки, незавершённый switch, повтор Stop, отмену/возврат установщика. Сохранённые credentials не дают автоматического права старта при отозванной сессии.
- [ ] Выполнить `cargo test -p nelomai-client-core -p nelomai-client-container --locked` и `npm run test:unit`; отревьюить все изменённые вызовы остановки.

**Приёмка:** stop для обновления не открывает ложный экран входа; состояния VPN и кнопок согласованы. Реальный logout остаётся logout.

### Задача 3. NLM-021: запрос плитки защищён от idle recycle

**Файлы:** `src-tauri/gen/android/app/src/main/java/ru/nelomai/client/NelomaiQuickTileService.kt`, `plugins/tunnel-android/android/src/main/java/NelomaiVpnService.kt`, при необходимости существующий общий runtime bridge в `plugins/runtime-android-common`.

**Тесты:** `src-tauri/gen/android/app/src/test/java/ru/nelomai/client/RuntimeDispatcherTest.kt`; создать `RuntimeTileRecycleTest.kt` рядом; использовать `plugins/tunnel-android/android/src/test/java/NelomaiVpnServiceTest.kt` и `QuickTunnelControllerTest.kt`.

**Вход:** принятое нажатие плитки до асинхронного выбора runtime. **Выход:** recycle учитывает незавершённый запрос до передачи engine.

- [ ] Воспроизвести управляемыми часами: stop назначает recycle; нажатие принято непосредственно перед дедлайном; callback выбора задержан; recycle не должен убить процесс.
- [ ] Ввести минимальный общий учёт принятого dispatch в том процессе, который проверяет recycle. Участок принятия запроса и решения об убийстве согласовать атомарно; не рассчитывать только на последующий `desiredActive`.
- [ ] Освобождать защиту на успехе передачи, отказе, отмене, уничтожении компонента и ограниченном timeout. Защита не должна навсегда отменять очистку idle-процесса.
- [ ] Проверить повторные нажатия, stop, смену runtime, поздний callback и обычную очистку после простоя; прогнать для WG и AWG.
- [ ] Запустить Android unit tests контейнера и tunnel plugin по действующей Gradle-конфигурации; записать точные команды и результаты.

**Приёмка:** совпадение третьего/следующего старта с recycle не срывает запуск; idle cleanup продолжает освобождать процесс.

### Задача 4. NLM-023: ограниченная очередь без падения VPN

**Файлы:** `plugins/runtime-android-common/android/src/main/java/ru/nelomai/client/RuntimeSelectionStore.kt`, `src-tauri/gen/android/app/src/main/java/ru/nelomai/client/RuntimeVpnDispatcherService.kt`.

**Тесты:** `src-tauri/gen/android/app/src/test/java/ru/nelomai/client/RuntimeDispatcherTest.kt`; создать рядом `RuntimeSelectionQueueTest.kt` с подключением общего исходника через существующую Gradle-конфигурацию.

**Вход:** периодическая проверка выбора runtime при недоступном Binder. **Выход:** ограниченное число ожидающих чтений, контролируемые ошибки и восстановление после возврата владельца.

- [ ] Воспроизвести более восьми ticks при неответившем owner: текущий `check(pending.size < 8)` падает.
- [ ] Исключить накопление одинаковых периодических чтений — один выполняющийся запрос проверки. Сохранить ограничение очереди для других потребителей, переполнение возвращать через Result/callback без исключения из служебного потока.
- [ ] Проверить политику `mustExitProcess`: временная ошибка связи не должна случайно стать доказанной сменой runtime через `getOrNull()`. При подтверждённой смене/отзыве сохранить остановку; неизвестное состояние не разрешает новый старт. Зафиксировать существующие требования к временному продолжению уже работающего туннеля и ограничению ожидания до изменения этой ветки.
- [ ] Проверить отключение Binder, повторное подключение, timeout, close/destroy и поздние callbacks: каждый callback завершается не более одного раза, очередь освобождается.
- [ ] Вместе с задачей 3 проверить pending tile dispatch во время недоступности owner и последующего восстановления; WG/AWG не должны падать от переполнения.
- [ ] Запустить Android unit tests; сравнить расход памяти/число pending за длительный синтетический простой owner.

**Приёмка:** нет `runtime_owner_queue_full` crash; число запросов ограничено, восстановление работает, допуск к новому runtime не обходится.

### Задача 5. NLM-024: убрать повторное полное хеширование broker

**Файлы:** `crates/contracts/src/dispatcher.rs`, `crates/unix-service/src/lib.rs` и фактический вызывающий код dispatcher, `crates/windows-service/src/windows/ipc.rs`.

**Тесты:** `crates/contracts/tests/dispatcher.rs`, существующие suites в `crates/unix-service/tests` и `crates/windows-service/tests`.

**Вход:** проверенная identity IPC-клиента и установленный broker. **Выход:** повторные запросы к неизменному доверенному executable не перечитывают весь файл.

- [ ] Снять базовый CPU dispatcher на Mac при активном и выключенном VPN за одинаковые интервалы; сохранить длительность, число запросов, версию и среднюю долю ядра.
- [ ] Закрепить тестом повторную авторизацию неизменного broker: счётчик полных digest должен стать 1 на одну неизменную policy/file identity, а не 1 на каждый запрос.
- [ ] Реализовать ограниченное кэширование успешной проверки в долгоживущем dispatcher. На каждом запросе сохранять проверку OS identity, точного пути, прав/владельца и идентичности файла. Привязать кэш к policy/manifest и файловой identity; изменение файла или policy инвалидирует запись. Не использовать один TTL или только mtime как доказательство неизменности.
- [ ] Проверять открытый файл и его метаданные до/после digest; смену файла между проверкой и использованием обрабатывать отказом/повторной верификацией. На Windows учитывать file identity и ACL, на Unix device/inode и защищённый путь. Ошибка чтения/метаданных не считается попаданием в кэш.
- [ ] Проверить подмену файла, rename replacement, смену policy, неверного владельца/путь, изменение прав и concurrent requests. Кэш не переносит доверие между разными пользователями/манифестами.
- [ ] Отдельно проверить обработку ошибок accept/обслуживания: при немедленно повторяющейся ошибке нужна ограниченная задержка и запись причины с ограничением частоты; штатные запросы задерживать не требуется.
- [ ] Выполнить `cargo test -p nelomai-contracts -p nelomai-unix-service -p nelomai-windows-service --locked`. Windows-specific tests выполнить на Windows runner: локальный macOS-прогон не подтверждает named pipes.
- [ ] Повторить замер Mac в тех же условиях; записать фактический эффект. Проверить Linux/Windows IPC, обновление broker и переход runtime без отключения проверок доверия.

**Приёмка:** повторные метрики не вызывают полное хеширование неизменного broker; средний CPU на Mac заметно снижается в сопоставимом замере; проверки подмены продолжают отказывать.

### Задача 6. NLM-019: подготовка диагностики и общее ревью

**Файлы:** `src-tauri/gen/android/app/src/main/java/ru/nelomai/client/RuntimeApkInstaller.kt`, `crates/client-container/src/update.rs`, `crates/client-container/src/host_updater.rs`, `src-tauri/src/updates.rs`, существующие журналы frontend/core. Документировать результат в `/Users/altzxd/Documents/Panel/RELEASE-0.2.20.md` при исполнении.

- [ ] Добавить недостающие события открытия установщика, возврата Activity, фаз update/switch, причины экрана входа и исходного кода provision failure. Возврат Activity сам по себе не объявлять отменой установки.
- [ ] Проверить, что события доступны в штатном отчёте и внешнем ADB-сборе; применить существующую ротацию и скрытие секретов.
- [ ] NLM-019 оставить открытой: отсутствие callback отмены доказано, окончательный отказ повторного открытия ещё не объяснён. Не заявлять закрытие всех её симптомов исправлением NLM-020.
- [ ] Провести совместное ревью: обновление + авторизация + late status; плитка + recycle + Binder queue; desktop trust cache + обновление пакета. Проверить WG/AWG, logout/revoke, latest/stable и сохранение LAN bypass.
- [ ] Исправить замечания и повторить проверки затронутой области; обновить реестр с разделением «реализовано», «проверено», «выпущено».

**Приёмка:** все пять исправлений прошли ревью; диагностика NLM-019 покрывает известные пробелы новых запусков и не раскрывает credentials.

### Задача 7. Подготовить и собрать подписанный кандидат 0.2.20

**Файлы:** `package.json`, `package-lock.json`, `src-tauri/tauri.conf.json`, Cargo manifests/lock, `src-tauri/gen/android/app/build.gradle.kts`, `.github/workflows/release.yml`, существующий встроенный fallback истории, `/Users/altzxd/Documents/Panel/RELEASE-0.2.20.md`.

- [ ] Применить существующий `python scripts/set-release-version.py 0.2.20`; согласовать все остальные закреплённые номера в workflow/contracts/Android и lock-файлах. Android versionCode — 2020, если фактический установленный code ниже; иначе сначала определить совместимый монотонный code.
- [ ] Сохранить одинаковый согласованный changelog в release input и fallback; после публикации проверить историю панели. Предлагаемый текст приведён ниже.
- [ ] Выполнить итоговые проверки действующего workflow:

```sh
npm test
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --locked
python contracts/python/validate_fixtures.py
python scripts/release-workflow-check.py
python -m unittest discover -s scripts/tests
git diff --check
```

- [ ] Подтвердить Android tests контейнера и tunnel plugin и native Windows checks; сохранить результаты, а не считать cross-compile аппаратной проверкой.
- [ ] Зафиксировать проверенный SHA, затем push согласно разрешению пользователя. Проверить remote SHA; запустить существующий release workflow с version 0.2.20, mode `sign_candidate`, этим source_sha. `critical=false`, `minimum_supported=false`; `panel_notification_ready` подтверждать текущим состоянием панели.
- [ ] При инфраструктурном сбое повторить failed jobs. Если нужен новый код — новый SHA и новый candidate; успешные сборки без причины не перезапускать.
- [ ] Проверить подписи, SHA256, Android package ID/versionCode/signer, комплектность четырёх платформ и принадлежность артефактов одному SHA. Сохранить candidate run ID.

**Приёмка:** подписанный кандидат готов для проверки, общедоступный релиз ещё не опубликован.

### Задача 8. Аппаратная проверка и расследование NLM-019

- [ ] На S23u до обновления проверить установленную 0.2.19, сохранить версию/code и начать непрерывный ADB Logcat в файл на Mac. Зафиксировать PID/exit reasons, состояние Activity и VPN; не очищать данные приложения.
- [ ] Обеспечить проверяемое предложение кандидата именно штатному updater на тестовом устройстве. Сначала проверить наличие существующего точечного механизма: публичный manifest не менять ради одного устройства. Если такого механизма нет, это отдельная контрольная точка для согласования минимального тестового способа доставки. `adb install -r` проверяет замену пакета, но не воспроизводит NLM-019.
- [ ] В 0.2.19 запустить VPN → начать обновление → отменить установку → вернуться в приложение → повторить открытие и отмену → открыть снова и установить 0.2.20. Сопоставить системный журнал, UI и переходы панели по времени/operation ID.
- [ ] Учесть, что до замены пакета работает старый код 0.2.19: диагностические изменения 0.2.20 не дополнят задним числом старый application log. Внешний ADB-сбор обязателен.
- [ ] После обновления проверить сохранение входа, доступность Start без перезапуска, ручной отчёт, фоновые задачи и счётчики.
- [ ] Выполнить не менее 10 start/stop циклов плитки для WG и 10 для AWG, включая нажатия около дедлайна recycle. Проверить handshake, двусторонний трафик, завершение stop и состояние плитки.
- [ ] Проверить временную недоступность owner на тестовом устройстве, в том числе воспроизводимый backup/заморозку при доступном способе эмуляции; после теста вернуть процесс в нормальный режим. Контролировать очередь, активный туннель, восстановление и запрет ошибочного нового старта.
- [ ] Исправление NLM-020 и собственный путь отмены из 0.2.20 проверить отдельно: подготовить тестовую цель обновления через согласованный точечный способ, остановиться на отмене без установки другой версии. Заранее 0.2.21 не публиковать. При отсутствии такого способа явно записать непроведённый аппаратный сценарий; unit/integration-проверку не называть аппаратной.
- [ ] На Mac проверить установку, запуск, меню/Dock, сохранность авторизации, WG/AWG и CPU dispatcher. На Windows/Linux выполнить доступную аппаратную проверку IPC/туннеля; отдельно записать, какие платформы проверены только native CI.
- [ ] Записать вывод по NLM-019: воспроизведена с причиной / новый частичный сигнал / не воспроизведена. Отсутствие повторения само по себе не закрывает историческую причину.

**Приёмка:** Android и Mac проверены; ограничения Windows/Linux и сценария обновления из 0.2.20 явно перечислены. Новый crash, потеря аккаунта, невозможность запуска/остановки или обход проверки trust блокируют публикацию до устранения.

### Задача 9. Публикация и завершение

- [ ] Сверить разрешение на публикацию, результаты аппаратной проверки, неизменность SHA/хешей кандидата и отсутствие уже опубликованной 0.2.20.
- [ ] Запустить `publish_approved_candidate` с сохранённым candidate run ID и тем же source_sha. Успешный кандидат не пересобирать.
- [ ] Проверить tag → SHA, Latest, полный набор assets, подписи/хеши, changelog и штатную синхронизацию панели.
- [ ] Проверить предложение обновления с 0.2.19, отсутствие предложения самому 0.2.20, историю «Что нового» и создание уведомления. Доставку push подтверждать отдельно от создания серверного события.
- [ ] Обновить `/Users/altzxd/Documents/Panel/KNOWN-ISSUES.md`, `HANDOFF.md` и `RELEASE-0.2.20.md`: что устранено и выпущено, чем проверено, какие пункты остаются открыты.
- [ ] После появления первых отчётов 0.2.20 проверить известные сигнатуры; при необходимости фонового наблюдения использовать штатную автоматизацию. Отсутствующие отчёты не трактовать как отсутствие ошибок.

## Предлагаемый changelog 0.2.20

- Исправлена недоступная кнопка запуска VPN после обновления приложения.
- Исправлено отображение подключения и авторизации при остановке VPN для обновления.
- Android: исправлен срыв запуска VPN из плитки быстрых настроек.
- Android: устранено падение VPN-службы при временной недоступности управляющего процесса.
- Снижена нагрузка на процессор фоновой службы на macOS, Windows и Linux.

Текст окончательно сверить с результатами исправлений и согласовать перед выпуском. NLM-019 не включать как полностью исправленную без доказательства; новые диагностические события не требуют отдельного пользовательского пункта.

## Самопроверка плана

- [x] Пять согласованных дефектов имеют отдельные изменения и критерии приёмки.
- [x] NLM-019 учитывает границу старой/новой версии и ограничение доставки непубличного кандидата.
- [x] Проверки включают WG/AWG, истинный logout/revoke, runtime switch и неизменность доверенного executable.
- [x] Порядок: исправления → ревью → подписанная сборка → устройство → публикация того же кандидата.
- [x] Изменения панели/агентов/kernel и чужие локальные изменения не включены в состав выпуска.
