[package]
name = "nelomai-client-tunnel"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
async-trait = "0.1"
ipnet = "2"
nelomai-contracts = { path = "../contracts" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
zeroize = "1"
