{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nelomai.ru/schemas/client/v1/common.schema.json",
  "$defs": {
    "apiVersion": { "const": "1" },
    "id": { "type": "string", "minLength": 1, "maxLength": 128 },
    "uuid": {
      "type": "string",
      "pattern": "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$"
    },
    "timestamp": { "type": "string", "format": "date-time" },
    "generation": {
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    },
    "layer": { "enum": ["tic", "stray"] },
    "transportProtocol": { "enum": ["wireguard", "amneziawg_3"] },
    "ticConnectionMode": { "enum": ["personal", "dynamic"] },
    "routeMode": { "enum": ["standalone", "via_tak"] },
    "egressMode": { "enum": ["ipv4", "prefer_ipv6"] },
    "platform": { "enum": ["android", "windows", "macos", "linux"] },
    "leaseStatus": {
      "enum": ["allocating", "issued", "connected", "warm", "released", "failed"]
    },
    "connection": {
      "type": "object",
      "required": [
        "lease_id",
        "pool_id",
        "layer",
        "tic_connection_mode",
        "route_mode",
        "egress_mode",
        "status",
        "pinned",
        "stopped_at"
      ],
      "properties": {
        "lease_id": { "$ref": "#/$defs/uuid" },
        "pool_id": {
          "anyOf": [
            { "$ref": "#/$defs/id" },
            { "type": "null" }
          ]
        },
        "layer": { "$ref": "#/$defs/layer" },
        "transport_protocol": { "$ref": "#/$defs/transportProtocol" },
        "tic_connection_mode": { "$ref": "#/$defs/ticConnectionMode" },
        "route_mode": { "$ref": "#/$defs/routeMode" },
        "egress_mode": { "$ref": "#/$defs/egressMode" },
        "status": { "$ref": "#/$defs/leaseStatus" },
        "pinned": { "type": "boolean" },
        "stopped_at": {
          "anyOf": [
            { "$ref": "#/$defs/timestamp" },
            { "type": "null" }
          ]
        }
      },
      "additionalProperties": true
    },
    "peerBinding": {
      "type": "object",
      "required": [
        "id",
        "peer_id",
        "interface_id",
        "interface_name",
        "slot",
        "preferred_layer",
        "tic_connection_mode",
        "route_mode",
        "egress_mode"
      ],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "peer_id": { "$ref": "#/$defs/id" },
        "interface_id": { "$ref": "#/$defs/id" },
        "interface_name": { "type": "string", "minLength": 1, "maxLength": 120 },
        "slot": { "type": "integer", "minimum": 1 },
        "preferred_layer": { "$ref": "#/$defs/layer" },
        "tic_connection_mode": { "$ref": "#/$defs/ticConnectionMode" },
        "route_mode": { "$ref": "#/$defs/routeMode" },
        "egress_mode": { "$ref": "#/$defs/egressMode" }
      },
      "additionalProperties": true
    },
    "probe": {
      "type": "object",
      "required": ["candidate_id", "measured_at"],
      "oneOf": [
        {
          "required": ["latency_ms"],
          "not": { "required": ["failure_code"] }
        },
        {
          "required": ["failure_code"],
          "not": { "required": ["latency_ms"] }
        }
      ],
      "properties": {
        "candidate_id": { "type": "string", "minLength": 20, "maxLength": 4096 },
        "latency_ms": { "type": "number", "exclusiveMinimum": 0, "maximum": 10000 },
        "failure_code": {
          "type": "string",
          "enum": [
            "invalid_url",
            "unsupported_scheme",
            "timeout",
            "network_error",
            "http_error",
            "unknown"
          ]
        },
        "measured_at": { "$ref": "#/$defs/timestamp" }
      },
      "additionalProperties": false
    }
  }
}
