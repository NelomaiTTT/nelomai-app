<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".NelomaiApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.nelomai_app"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <provider android:name=".KeyringPreferencesProvider"
            android:authorities="${applicationId}.keyring-preferences"
            android:exported="false" />
        <!-- Wry observes ProcessLifecycleOwner in :runtime, not in the broker process.
             Keep AndroidX's eager initialization beside both runtime UI variants. -->
        <provider android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false" android:process=":runtime" />
        <service android:name=".RuntimeAuthBrokerService" android:exported="false" />
        <activity android:name=".RuntimeApkInstallerActivity" android:exported="false" />
        <service android:name=".RuntimeVpnDispatcherService" android:exported="false"
            android:foregroundServiceType="systemExempted" android:permission="android.permission.BIND_VPN_SERVICE"
            android:process=":vpn" android:stopWithTask="false">
            <intent-filter><action android:name="android.net.VpnService" /></intent-filter>
        </service>
        <activity android:name=".RuntimeBootstrapActivity" android:exported="false" android:process=":runtime" />
        <activity android:name=".RuntimeRestartActivity" android:exported="false"
            android:excludeFromRecents="true" android:launchMode="singleTask"
            android:taskAffinity="${applicationId}.runtime_restart" />
        <activity android:name=".LatestRuntimeActivity" android:exported="false" android:process=":runtime"
            android:launchMode="singleTask"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode" />
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name=".NelomaiQuickTileService"
            android:exported="true"
            android:icon="@drawable/ic_quick_tile"
            android:label="@string/app_name"
            android:process=":vpn"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
